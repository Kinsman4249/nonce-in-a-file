# Session notes
handoff.md
todo.md
.claude/
.kilo/
.kilo

# CI-staged builder copy: the deploy workflow injects the LEARN_MORE_URL
# variable into builder-dist/index.html, then deploys it. Never commit it.
builder-dist/